const { BOT_TOKEN } = require('./bot-token');
const { SECRET_KEY } = require('./secret-key');
const { WHITELISTED_ORIGINS } = require('./whitelisted-origins');


module.exports = { BOT_TOKEN, SECRET_KEY, WHITELISTED_ORIGINS };
